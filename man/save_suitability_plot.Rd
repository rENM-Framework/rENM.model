% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-save_suitability_plot.R
\name{save_suitability_plot}
\alias{save_suitability_plot}
\title{Save a ggplot suitability plot to file}
\usage{
save_suitability_plot(
  p,
  fn,
  width = 1000,
  height = 750,
  res = 72,
  pointsize = 15,
  title_size = 28,
  subtitle_size = 20,
  legend_size = 15,
  legend_title_size = 15,
  axis_title_size = 20,
  axis_text_size = 18
)
}
\arguments{
\item{p}{ggplot. A ggplot object to save.}

\item{fn}{Character. Full output path and file name, with or without an
extension. If no extension is provided, ".png" is appended.}

\item{width}{Numeric. Image width in pixels. Default 1000.}

\item{height}{Numeric. Image height in pixels. Default 750.}

\item{res}{Numeric. Resolution (DPI). Default 72.}

\item{pointsize}{Numeric. Base point size for text passed to ggsave().
Default 15.}

\item{title_size}{Numeric. Font size for the plot title. Default 28.}

\item{subtitle_size}{Numeric. Font size for the plot subtitle. Default 20.}

\item{legend_size}{Numeric. Font size for legend text. Default 15.}

\item{legend_title_size}{Numeric. Font size for the legend title. Default 15.}

\item{axis_title_size}{Numeric. Font size for axis titles. Default 20.}

\item{axis_text_size}{Numeric. Font size for axis tick labels. Default 18.}
}
\value{
Invisibly returns the file path written.
\itemize{
  \item Character. File path to the saved plot.
  \item Side effect: Writes an image file to disk.
}
}
\description{
Save a ggplot object to disk with configurable size, resolution, and
typography. File format is inferred from the extension or defaults to PNG.
}
\details{
This function is part of the rENM framework's processing pipeline
and operates within the project directory structure defined by
rENM_project_dir().

\strong{Pipeline context}
This function saves ggplot outputs generated during the rENM workflow,
resolving relative paths against the project directory defined by
rENM_project_dir().

\strong{Inputs}
\itemize{
  \item A ggplot object.
  \item An output file path, with or without extension.
}

\strong{Outputs}
\itemize{
  \item A saved image file written to disk.
  \item If no extension is provided, a PNG file is written.
}

\strong{Methods}
Width and height are specified in pixels and converted to inches using
the resolution (DPI) for ggplot2::ggsave().

Supported extensions:
\itemize{
  \item "png", "pdf", "tiff", "jpg", "jpeg", "svg", "eps"
}

Before saving, the plot theme is enhanced to apply specified font sizes
to the title, subtitle, legend, and axis text elements.

\strong{Data requirements}
The output path is resolved relative to the project directory if a
non-absolute path is provided.
}
\examples{
\dontrun{
  p <- ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt)) +
         ggplot2::geom_point()
  save_suitability_plot(p, "plots/mtcars_scatter.png")

  # No extension provided -> defaults to PNG:
  save_suitability_plot(p, "plots/mtcars_scatter")
}

}
\seealso{
[ggplot2::ggsave()], [ggplot2::theme()],
  [ggplot2::element_text()]
}
